allOf:
  - $ref: '../../schemas.yml#/collection_response'
  - type: object
    properties:
      streams:
        items:
          $ref: '../../schemas.yml#/public_stream_config'
        type: array
        description: List of fetched streams according to the query parameters.
