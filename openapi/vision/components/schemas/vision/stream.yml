type: object
properties:
  name:
    type: string
    format: media_name
    description: Globally unique stream name.
    examples:
      default:
        value: "cam-entrance-2"
      one:
        value: "backyard"

  vision:
    allOf:
    - $ref: ../../schemas.yml#/stream_config
