type: object
properties:
  url:
    description: |
      URL to connect to the source and get the feed of events.
    type: string
    format: url
    examples:
      default:
        value: 'https://vision-01.example.com/vision/api/v3/events'

required:
- url

